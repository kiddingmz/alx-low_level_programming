#include "main.h"

/**
 * _abs - Absolute value of an integer
 *
 * @r: Receive the number to get absolute value
 *
 * Return: absolute number
 */

int _abs(int r)
{
	return ((r < 0) ? (-1 * r) : r);
}
